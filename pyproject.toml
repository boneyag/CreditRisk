[project]
name = "creditrisk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "scikit-learn>=1.8.0",
    "statsmodels>=0.14.6",
    "xgboost-cpu>=3.2.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]